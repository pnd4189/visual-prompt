Explicitly invoke the native `$visual-prompt` skill with the complete user
argument string below. Do not replace it with an ad-hoc workflow.
Preserve the default image-only behavior. Enable video/music only when the user
passed the corresponding flag or clearly requested that medium.

User arguments:
$ARGUMENTS
