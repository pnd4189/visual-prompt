---
description: Generate grounded QA and image prompts from a Vietnamese novel
argument-hint: <input.txt> [--no-video] [--no-music]
---

Invoke the native `$visual-prompt` skill with the complete user argument string
below. Preserve QA + image-only behavior unless the user explicitly requests
video or music; `--no-video` and `--no-music` always win.

User arguments:
$ARGUMENTS
